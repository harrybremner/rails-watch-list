<p><strong>Movies in the <%= @list.name %> list </strong></p>
<ul class="list-group">
  <% @list.bookmarks.each do |bookmark| %>
    <li class="list-group-item" style = "background: url(<% bookmark.movie.poster_url %>) no-repeat bottom center;"> <%= bookmark.movie.title %>
    <br>
    <p>Overview:
    <%= bookmark.movie.overview %></p>
    <p>Rating:
    <%= bookmark.movie.rating %></p></li>
    <br>
      <%= link_to "Delete",
    bookmark_path(bookmark) ,
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
  %>
  <% end %>
</ul>
<br>


<%= link_to "Add a movie to this list?", new_list_bookmark_path(@list), class: "btn btn-primary" %>

<br>
<br>

<%= link_to "Home", root_path(), class: "btn btn-primary" %>
