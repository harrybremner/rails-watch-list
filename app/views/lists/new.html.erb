<h1>New Movie List</h1>
<%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.submit "Submit movie list", class: "btn btn-primary" %>
<% end %>

<br>

<%= link_to "Home", lists_path(@lists), class: "btn btn-primary" %>
